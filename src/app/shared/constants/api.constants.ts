export const baseUrl = `https://restcountries.com/v2`;

export const countriesApi = `${baseUrl}/all`;

export const countryApi = `${baseUrl}/name`;

export const regionApi = `${baseUrl}/region`;

export const codeApi = `${baseUrl}/alpha`;
